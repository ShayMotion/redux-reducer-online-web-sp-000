export function managePresents(state, action){
  let numberOfPresents = {numberOfPresents: 0};
    switch (numberOfPresents.type) {
    case 'INCREASE_COUNT':
      return {count: state.count + 1};
  }
}
